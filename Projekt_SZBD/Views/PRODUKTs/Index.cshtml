@model IEnumerable<Projekt_SZBD.Models.PRODUKT>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Nazwa)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Cena_Kupno)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Cena_Sprzedaz)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Nazwa)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Cena_Kupno)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Cena_Sprzedaz)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID_Produktu }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID_Produktu }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID_Produktu })
        </td>
    </tr>
}

</table>
