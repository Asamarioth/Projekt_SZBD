@model IEnumerable<Projekt_SZBD.Models.EGZEMPLARZ>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Ilosc)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.MAGAZYN.Opis)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.PRODUKT.Nazwa)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Ilosc)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.MAGAZYN.Opis)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.PRODUKT.Nazwa)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID_Egzemplarza }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID_Egzemplarza }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID_Egzemplarza })
        </td>
    </tr>
}

</table>
